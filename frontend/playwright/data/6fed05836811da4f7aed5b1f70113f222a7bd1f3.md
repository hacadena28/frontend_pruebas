# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> AuthGuard E2E Tests >> should redirect unauthenticated users to login page
- Location: e2e\auth.spec.ts:5:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /.*\/login/
Received string:  "http://localhost:4200/vehicles"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://localhost:4200/vehicles"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('AuthGuard E2E Tests', () => {
  4  |   
  5  |   test('should redirect unauthenticated users to login page', async ({ page }) => {
  6  |     // Attempt to access a protected route
  7  |     await page.goto('/vehicles');
  8  | 
  9  |     // Verify that the AuthGuard intercepted and redirected to /login
> 10 |     await expect(page).toHaveURL(/.*\/login/);
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  11 |     
  12 |     // Optionally verify that the login form is visible
  13 |     // Update the selector if your login page uses different elements
  14 |     // const loginHeading = page.getByRole('heading', { name: /login/i });
  15 |     // await expect(loginHeading).toBeVisible();
  16 |   });
  17 | 
  18 | });
  19 | 
```